package com.example.domain.choiseCountry.model

data class MainData(
    val listData: List<TicketsOffer>,
) {
//    fun mapToUi() = ChoiceCountryUi(
//        itemId = listData.map { it.id }.first().toString(),
//        id = listData.map { it.id }.first(),
//        title = listData.map { it.title }.first(),
//        timeRange = listData.map { it.timeRange }.first(),
//        price = listData.map { it.price }.first(),
//    )
}
